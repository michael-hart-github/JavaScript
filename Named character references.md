Using [[HTML entities]] are a kind of character reference.

The character references shown in the [[HTML entity example]] are known as named character references.

Named character references start with an ampersand (`&`) and end with a semi-colon (`;`).